# Project-specific .Rprofile
# This file prevents the system-wide .Rprofile from loading

# Minimal setup for package development
if (interactive()) {
  suppressMessages(require(devtools, quietly = TRUE))
}
